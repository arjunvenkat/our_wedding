<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>">Kriti and Arjun</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        <li><a href="<%= gallery_path %>">Photos</a></li>
        <li><a href="<%= root_url(anchor: 'accomodations') %>">Accomodations</a></li>
        <% if session[:household_id].present? && current_household %>
          <li><a href="<%= rsvp_status_household_path(current_household.id, current_household.unique_hex) %>">RSVP status and event info</a></li>
          <li><a data-method="delete" href="<%= logout_household_path(current_household.id) %>">Log out</a></li>

        <% else %>
          <li><a href="<%= root_url(anchor: 'rsvp') %>">RSVP</a></li>
        <% end %>

        <% if current_user.present? %>
          <li><a href="<%= households_path %>">Households</a></li>
          <li><a href="<%= guests_path %>">Guests</a></li>
          <li><a href="<%= events_path %>">Events</a></li>
          <li><a href="<%= rsvps_path %>">RSVPs</a></li>
          <li><a data-method="delete" href="<%= destroy_user_session_path %>">Log out as admin</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
